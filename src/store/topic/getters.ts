import { GetterTree } from 'vuex';
import { TopicState } from './interface';
import { RootState } from '@/store/interface';

const getters: GetterTree<TopicState, RootState> = {
};

export default getters;
